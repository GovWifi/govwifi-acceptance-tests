# This test simulates a client configuration error where the certificate and private key do not match.
network={
        ssid="GovWifi"
        key_mgmt=WPA-EAP
        eap=TLS
        identity="tstusr"
        ca_cert="/usr/src/app/certs/ca.pem"
        client_cert="/certificates/client.pem"
        private_key="/certificates/root_ca.key" # <-- Wrong key
        private_key_passwd="whatever"
}